---
import { cn } from '../../lib/utils';
import { pageShell } from '../../lib/classes';

const { as = 'section', class: className = '', ...attrs } = Astro.props;
const Tag = as;
---

<Tag class={cn(pageShell, className)} {...attrs}>
  <slot />
</Tag>
